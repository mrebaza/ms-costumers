version: "3.9"
services:
  spring-api-image:
    image: ms-customers
    container_name: ms-customers
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8081:8081"
    networks:
      - spring-network

networks:
  spring-network:
#volumes:
#  mysql-volume: